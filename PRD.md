# 📝 PRD: 싱글 페이지 기반 가벼운 운동 기록 서비스

## 1) 목표와 성공 조건
*   **목표:** 운동 중 복잡한 설정이나 화면 전환 없이, 직관적인 To-do 리스트처럼 빠르고 가볍게 루틴과 세트/무게를 기록할 수 있는 단일 화면 클라이언트 웹 애플리케이션(SPA) 구축. 백엔드, DB, 로그인을 철저히 배제하고 로컬 상태만 활용함.
*   **성공 조건:** 사용자가 앱 진입 후 1분 이내에 3분할을 선택하고, 운동 3개를 추가하여 세트/무게를 입력한 뒤, 태그 1개를 수동으로 수정하고, '가슴' 부위만 필터링해서 보는 모든 과정이 '새로고침 전'까지 클라이언트 단에서 어떠한 버그 없이 매끄럽게 동작하는 것.

## 2) 사용자 스토리
*   나는 헬스인으로서, 상단 탭에서 내 루틴(예: 3분할)을 바로 선택해 운동 기록을 시작하고 싶다.
*   나는 운동 중 흐름이 끊기지 않도록, To-do 리스트처럼 버튼 하나로 운동 항목을 추가하고, 불필요한 항목은 지우며, 순서를 드래그 앤 드롭으로 편하게 바꾸고 싶다.
*   나는 운동 이름을 입력하면 앱이 알아서 부위(가슴, 등 등)를 태그로 달아주어 분류해 주길 바란다.
*   나는 운동 항목을 눌렀을 때만 하단에 세트, 횟수, 무게를 입력하는 칸이 펼쳐져 화면을 깔끔하게 유지하고 싶다.
*   나는 화면 상단의 부위 칩(Chip)을 눌러 특정 부위의 운동만 모아서 보고 싶다.

## 3) 화면 명세
본 애플리케이션은 **오직 단일 화면(Single Page)** 으로 구성되며, 스크롤 가능한 상하 구조를 가집니다.

*   **상단 영역 (Split Selector):** '무분할', '2분할', '3분할', '4분할', '5분할'을 선택할 수 있는 퀵 선택 탭 UI.

*   **분할 영역 (Split Selector):** 분할에 따라 일별로 운동기록할수있는 탭 UI.

*   **필터 영역 (Tag Filter):** '전체' 및 7개 부위(가슴, 등, 어깨, 삼두, 이두, 전완, 하체)를 선택할 수 있는 가로 스크롤 형태의 칩(Chip) 버튼 리스트.
*   **메인 리스트 영역 (Dynamic List):**
    *   **개별 운동 아이템 (Row):** 좌측 드래그 핸들 ☰, 운동명 입력 텍스트 필드, 자동 생성된 부위 태그(버튼형), 우측 삭제(x) 버튼으로 구성.
    *   **아코디언 영역 (Expanded 폼):** 아이템 터치 시 하단으로 확장되며, 세트 번호, 무게(kg) 입력칸, 횟수(reps) 입력칸, 세트 삭제 버튼, 그리고 '세트 추가(+)' 버튼 표출.
*   **하단 영역 (Floating Action):** 화면 하단에 고정된 커다란 '운동 추가 (+)' 버튼.

## 4) 기능 명세
*   **분할 선택 (상단 탭):** 탭 클릭 시 활성화 상태가 변경되며, 이는 단순 시각적 표시 및 세션 내 상태로 유지됨.
*   **동적 리스트 상태 관리:** 리스트 항목은 배열 형태로 클라이언트 메모리에서 관리됨.
    *   추가(+): 배열의 맨 끝에 빈 운동 객체 추가.
    *   삭제(x): 배열에서 해당 객체 제거.
    *   순서 변경: Drag & Drop 이벤트를 통해 배열 내 객체 인덱스 스왑(Swap).
*   **가짜 AI (Mock-AI) 태깅 시스템:**
    *   사전에 하드코딩된 단어 딕셔너리(Key: 검색어, Value: 부위)를 사용.
    *   운동명 텍스트 필드에 onChange 이벤트 발생 시 문자열을 검사하여 일치하는 키워드가 있으면 해당 객체의 태그 값을 자동 업데이트함.
*   **태그 수동 편집:** 자동 생성된 태그 UI를 터치하면, 7개 부위(가슴, 등, 어깨, 삼두, 이두, 전완, 하체)를 선택할 수 있는 작은 모달 또는 드롭다운이 노출되어 사용자가 값을 강제 덮어쓰기 할 수 있음.
*   **아코디언 토글 로직:** 각 운동 객체는 `isExpanded` boolean 상태를 가지며, 터치 시 토글됨. 확장 시 내부 배열(세트 데이터) 렌더링.
*   **부위별 필터링:** 필터 칩 클릭 시, 메인 리스트 배열 렌더링을 원본 배열이 아닌 `운동객체.tag === 선택된필터` 인 항목만 노출하도록 처리 (선택된 필터가 '전체'인 경우는 모두 노출).

## 5) 예외 처리 (Exception Handling) 
*(※ 구현 시 가장 중점적으로 반영해야 할 필수 방어 로직)*

1.  **태그 매칭 실패 및 수동 변경 예외:**
    *   사용자가 딕셔너리에 없는 모호한 운동명을 입력하여 태그가 매칭되지 않거나, 딕셔너리 한계로 잘못 매칭된 경우 오류가 발생해선 안 됨.
    *   **해결:** 매칭 실패 시 기본값인 '미지정' 텍스트를 가진 태그를 표출함. 사용자가 이 태그를 탭할 경우 드롭다운을 띄워 7가지 부위 중 하나로 수동 강제 변경할 수 있도록 처리함. 수동 변경된 태그는 이후 텍스트가 수정되더라도 자동 매칭 로직보다 높은 우선순위를 가져 덮어써지지 않게 `isManualTagged` 플래그로 보호함.
2.  **운동명 누락 시 세트/무게 입력 시도 예외:**
    *   사용자가 운동명을 전혀 입력하지 않은 빈칸 상태에서, 하단 아코디언을 열어 세트나 무게부터 입력하려 할 경우 데이터 구조상 식별이 모호해지는 문제 발생.
    *   **해결:** 운동명이 비어있는 상태에서 아코디언 내부의 텍스트 필드(무게/횟수)에 포커스를 주려 할 경우 입력을 일시 차단함. 동시에 사용자 인지를 위해 비어있는 '운동명 텍스트 필드'에 붉은색 테두리를 1.5초간 깜빡이게(Blink) 표출하고, 해당 필드로 포커스를 강제 이동시킴.
3.  **마지막 남은 운동 항목 삭제 시도 예외:**
    *   앱 진입 시 기본적으로 1개의 운동 항목이 존재하는데, 리스트에 운동 항목이 단 1개만 남았을 때 사용자가 'x(삭제)' 버튼을 눌러버리면 화면이 텅 비어버려(Empty State) 추가 동작의 직관성이 떨어지는 문제 발생.
    *   **해결:** 배열의 `length`가 1일 때 삭제 버튼이 눌린 경우, 배열에서 객체를 삭제(splice/remove)하는 대신 해당 객체의 모든 값을 빈칸(운동명 `""`, 태그 `미지정`, 세트 데이터 초기화)으로 리셋(초기화)하는 로직으로 우회 처리함. 
4.  **무게 및 횟수 비정상 입력 차단 예외:**
    *   숫자가 들어가야 할 무게나 횟수 입력 칸에 문자열이나 기호가 들어가면 연산이나 뷰 렌더링 시 NaN 에러 등 오류 발생 가능성 존재.
    *   **해결:** 해당 입력 필드는 오직 숫자(0-9)와 무게 필드에 한해 소수점(.) 1개까지만 허용하도록 정규식 마스킹 처리함. 특히, 빼기/음수 기호(`-`)나 스페이스바 입력을 감지하면 이벤트 기본 동작을 취소(`preventDefault()`)하여 아예 입력 자체가 되지 않도록 원천 차단함.
5.  **필터링 활성화 상태에서 새 운동 추가 시 노출 예외:**
    *   예를 들어 '가슴' 필터가 켜져 있어 가슴 운동만 보이는 상태일 때, 하단의 '+' 버튼을 눌러 새 운동(기본값 미지정 상태)을 추가하면 새 항목이 렌더링 필터 조건에 맞지 않아 화면에 보이지 않고 "추가가 안 되네?"라고 오인할 수 있음.
    *   **해결:** 하단의 '+' 버튼을 눌러 새 운동 항목이 배열에 추가되는 즉시, 앱의 현재 필터 상태를 강제로 '전체'로 초기화시킴. 화면 전체 리스트가 다시 렌더링되면서 새로 추가된 항목이 리스트의 가장 하단에 정상적으로 노출되도록 강제함.

## 6) 완료 조건
*   어떠한 외부 백엔드 API, DB, 사용자 인증 로직도 포함되어 있지 않아야 함.
*   새로고침 시 데이터가 날아가는 휘발성 로컬 상태(클라이언트 메모리)로 완전히 구동되어야 함.
*   SPA 환경에서 단일 화면 모달/아코디언만으로 화면 전환 없이 모든 동작이 수행되어야 함.
*   상기 명시된 5가지 예외 처리 로직이 코드 내에 빠짐없이 조건문/이벤트 핸들러 형태로 구현되어야 함.