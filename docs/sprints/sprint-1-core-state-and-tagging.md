# 🏃 Sprint 1: 코어 상태 모델 및 Mock-AI 태깅 시스템 구축

## 📋 스프린트 목표
PRD 요구사항에 따라 7개 부위(가슴, 등, 어깨, 삼두, 이두, 전완, 하체)를 자동 분류하는 Mock-AI 딕셔너리 시스템을 구축하고, 상태 구조를 PRD에 부합하도록 확장합니다.

---

## 🛠️ 세부 작업 항목 (Tasks) 및 진행 현황

### 1. 데이터 모델 업데이트 ([`lib/workout-data.ts`](../../lib/workout-data.ts))
- [x] `Exercise` 타입에 `isManualTagged: boolean` 필드 추가
- [x] `createEmptyExercise` 헬퍼에 `isManualTagged: false` 기본값 반영
- [x] 기본 리셋 헬퍼 `resetExerciseItem` 함수 구현

### 2. Mock-AI 키워드 매핑 딕셔너리 구축 ([`lib/workout-data.ts`](../../lib/workout-data.ts))
- [x] 7개 부위별 주요 운동 키워드 사전 구축 (`WORKOUT_KEYWORD_DICT`):
  - **가슴:** 벤치, 벤치프레스, 인클라인, 디클라인, 체스트프레스, 펙덱플라이, 딥스, 푸쉬업, 케이블크로스 등
  - **등:** 랫풀다운, 풀업, 턱걸이, 친업, 바벨로우, 덤벨로우, 시티드로우, 데드리프트, 암풀다운, 티바로우 등
  - **어깨:** 오버헤드프레스, OHP, 밀리터리프레스, 사레레, 사이드레터럴, 레터럴레이즈, 프론트레이즈, 페이스풀, 숄더프레스 등
  - **삼두:** 트라이셉스, 푸시다운, 익스텐션, 라잉트라이셉스, 스컬크러셔, 킥백, 클로즈그립 등
  - **이두:** 바벨컬, 덤벨컬, 해머컬, 프리처컬, 인클라인컬, 컨센트레이션컬 등
  - **전완:** 리스트컬, 리버스리스트컬, 추감기, 악력 등
  - **하체:** 스쿼트, 백스쿼트, 프론트스쿼트, 레그프레스, 레그익스텐션, 레그컬, 런지, 힙쓰러스트, 카프레이즈 등
- [x] `matchMuscleByKeyword(name: string): MuscleGroup | null` 유틸리티 함수 구현 (긴 키워드 우선 정렬 매칭)

### 3. 운동명 입력과 Mock-AI 태깅 연동 ([`components/exercise-row.tsx`](../../components/exercise-row.tsx))
- [x] 운동명 `onChange` 시 `isManualTagged`가 `false`인 경우에만 실시간 자동 태깅 실행
- [x] 일치하는 키워드가 없으면 `null`(미지정)로 설정
- [x] 사용자가 태그 픽커에서 부위를 수동 선택한 경우 `isManualTagged: true`로 설정하여 자동 덮어쓰기 방지

---

## ✅ 완료 검증 (Verification Results)
1. **Mock-AI 자동 분류:** "벤치프레스" 입력 시 즉시 "가슴", "스쿼트" 입력 시 즉시 "하체", "랫풀다운" 입력 시 즉시 "등"으로 자동 태깅 확인 완료.
2. **수동 태그 보호 (`isManualTagged`):** 사용자가 태그를 "어깨"로 수동 변경한 후, 운동명을 "벤치프레스"로 다시 고쳐도 "어깨" 태그가 유지됨을 확인.
3. **태그 미지정 처리:** 키워드 매칭이 없을 시 '미지정' 태그 칩으로 안전하게 렌더링 확인.
4. **빌드 및 런타임 상태:** 0 error, 로컬 서버 정상 구동 확인.
